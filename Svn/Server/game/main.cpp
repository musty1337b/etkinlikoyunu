//Yoksa Ekle:

#	include "minigame.h"

//ARat:

	if (!g_bAuthServer && g_bChannel == CHANNEL_99)
	{
		if (!(pulse % (passes_per_sec)))
		{


//Ekle :

#ifdef ENABLE_MINI_GAME_FINDM
			CMiniGame::Instance().MiniGameFindMCheckEnd();
#endif